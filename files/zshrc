# Path to your oh-my-zsh configuration.
ZSH=$HOME/.oh-my-zsh

# Set name of the theme to load.
# Look in ~/.oh-my-zsh/themes/
# Optionally, if you set this to "random", it'll load a random theme each
# time that oh-my-zsh is loaded.
ZSH_THEME="theunraveler"

# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
alias roll="cd /Users/RoadHouse/code/ruby/love2roll"
function _basedir {
  basename `pwd`
}

alias ls="ls -G"
alias s="rails s"
alias c="rails c"
alias g="git"
alias cvr="rake spec:rcov && open coverage/index.html"
alias td="tail -f log/development.log"
alias tt="tail -f log/test.log"
alias ensuremigrate="rake db:migrate && rake db:rollback && rake db:migrate && rake db:test:prepare"
alias recreate_all="rake db:drop:all && rake db:create:all && rake db:migrate && rake db:seed && rake db:test:prepare"
alias bndj='bundle install --binstubs=bin_stubs --path=~/gems --without=production'
alias fs='foreman start'

_rmate() {
  $(ls -1 | egrep -v "(diagrams|coverage|log|git|vendor|tmp|doc)" | egrep -v .DS_Store | xargs mate)
}
alias rmate="_rmate"

alias gmi='gem install'
alias ackr='ack --ruby'
alias res='touch tmp/restart.txt'
alias pg.start='pg_ctl -D /usr/local/var/postgres -l /usr/local/var/postgres/server.log start'
alias mongo.start='mongod run --config /usr/local/Cellar/mongodb/2.0.1-x86_64/mongod.conf'

# Set to this to use case-sensitive completion
# CASE_SENSITIVE="true"

# Comment this out to disable weekly auto-update checks
DISABLE_AUTO_UPDATE="true"

# Uncomment following line if you want to disable colors in ls
# DISABLE_LS_COLORS="true"

# Uncomment following line if you want to disable autosetting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment following line if you want red dots to be displayed while waiting for completion
COMPLETION_WAITING_DOTS="true"

unsetopt correct_all

# bindkey -e

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(git brew cap github heroku ruby vi-mode zsh-syntax-highlighting)

source $ZSH/oh-my-zsh.sh

# Customize to your needs...
export PATH=./bin_stubs:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:/usr/local/sbin:~/.rvm/bin:/usr/local/Cellar/ruby/1.9.3-p327/bin:$PATH

# set x86_64 flags
# export CFLAGS="-arch x86_64 -O2"
# export ARCHFLAGS="-arch x86_64"
export EDITOR=vim
export CDPATH=.:~/code/work/cinemateca/:~/code/pet_projects/
export HISTIGNORE="&:[bf]g:exit"
export HISTCONTROL=ignoredups
export HISTCONTROL=ignoreboth
export GREP_OPTIONS="--color=auto"
export GREP_COLOR="4;33"
export CLICOLOR="auto"

export TERM='xterm-256color'


PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting
